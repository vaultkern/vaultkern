{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the VaultKern resident window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
